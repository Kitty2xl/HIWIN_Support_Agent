# Backend config. The DB / image / inference-host values are SHARED with the
# pipeline (pipeline/settings.json): edit them there and run
#     python env_from_settings.py
# which rewrites only those shared keys here and leaves the rest alone.
# Verify the whole stack with:  python doctor.py

# --- Inference server (llama.cpp router, OpenAI-compatible endpoint) ---
INFERENCE_HOST=http://localhost:11400
LANGUAGE_MODEL=Support_Agent_Qwen3.6
EMBEDDING_MODEL=Embedding_Qwen3.6
RERANKER_MODEL=Reranker_Qwen3.6

# --- Postgres (must match what the pipeline wrote) ---
DB_NAME=hiwin_rag_db
DB_USER=postgres
DB_PASSWORD=rootpassword
DB_HOST=localhost
DB_PORT=5432
DB_SCHEMA=hiwin_rag
DB_SSLMODE=prefer

# --- Images (same folder the pipeline copies figures into) ---
IMAGE_STATIC_ROOT=C:\Users\User_11\Desktop\HIWIN\web_static\HIWIN

# --- Chat logging ---
CHAT_LOG_ENABLED=true

# --- Separate frontend (see docs/API.md) ---
# Allowed browser origins for /chat, comma-separated; * = any; empty = no CORS headers.
CORS_ALLOW_ORIGINS=*
# Set to the URL clients reach this backend at (e.g. http://10.0.0.5:8079) to make the
# image links in answers absolute. Empty = keep /static/HIWIN/... relative (same-origin only).
PUBLIC_BASE_URL=

# --- Answer quality / latency tuning (backend only; see README "Configuration") ---
COMPLETENESS_MODE=pre_draft
KEEP_RECENT_TOOL_RESULTS=-1
GRADING_ENABLED=false
COMPLETENESS_PASS_ENABLED=true
CHAT_TIMEOUT=30000
COMPLETENESS_MODEL=Support_Agent_Aux
COMPLETENESS_CONCURRENCY=4
# Measured: 20 candidates x 2000 chars = ~9.7k tokens per rerank call, which is
# what makes reranking cost ~15s on a partially-offloaded model. Halving the
# per-doc budget halves that prefill; the ranking signal is almost always in the
# opening of a passage. Full passages still go to the LLM either way.
RERANK_DOC_MAX_CHARS=1000
